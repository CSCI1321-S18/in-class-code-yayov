package College

class section(val course: course) {
  private var students: List[student] = Nil
  
 // def addStudent(s:student): Boolean = {
   // if(students.length < capacity) {
      //TODO add student
   // } else false
//  }
}